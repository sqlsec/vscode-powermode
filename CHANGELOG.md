# Changelog

本项目的所有重要变更都会记录在此文件。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.0] - 2026-09-10

首个公开版本。

### 新增

- 打字特效：每次输入（含退格删除）在光标处迸发粒子。
- 5 种特效风格预设：`particles`、`fireworks`、`flames`、`magic`、`explosion`，数值对齐 POWERMODE 原版手感。
- 3 种配色模式：`code`（跟随代码语法高亮颜色）、`rainbow`（随机彩虹）、`fixed`（固定颜色）。
- 跟随代码取色：解析当前主题的 `tokenColors`（含 `include` 继承链），把光标处的语法类型映射为实际颜色；识别注释、字符串、数字、关键字、类型、函数、属性、运算符、标点、常量、变量。
- 连击加成：连续快速输入时粒子数量按级递增，倍率上限可配（`powermode.combo.maxMultiplier`）。
- 状态栏指示器：只占一个图标（`⚡` 开启 / `⊘` 关闭），单击即可开关，悬停查看当前风格 / 配色 / 最近取色。
- `Power Mode: 选择颜色` 命令：弹出色板（14 种常用色 + 自定义输入），选完自动把 `powermode.colorMode` 切到 `fixed`，不必再手填色值。（VS Code 设置界面不会为普通字符串项渲染色块，`format: color-hex` 只在编辑 `settings.json` 时提供原生取色器，因此补一个命令作为入口。）
- 输入法兼容：检测到 IME 组字时暂停特效与渲染，输入停顿后补发一次，避免打断候选框定位。
- 6 个命令：开启/关闭、开启、关闭、选择特效风格、选择颜色、查看当前语法的取色结果。
- 可选的行抖动效果（`powermode.shake`，默认关闭）。
- 11 项配置项，覆盖风格、配色、帧率、连击、行抖动、状态栏与语言排除。

### 变更

- 状态栏从「文字 + 连击倍率」改为单图标，风格、配色、最近取色收进悬停提示；同时移除提示里的「连击」与「存活粒子」两项。
- 精简配置项，移除以下不直观 / 少用的设置（一律固定为原默认值，行为不变）：
  - `powermode.particles.countMin` / `countMax` / `sizeMin` / `sizeMax` / `gravity` / `decay`：直接用所选风格的内置数值。
  - `powermode.particles.max`：固定为 200。
  - `powermode.combo.step`：固定为 12（每 12 个字符 +0.25）。
  - `powermode.combo.timeout`：连击空闲判定改由既有的输入窗口统一负责，输入一停即清零。

### 性能

面向低端笔记本（i5-1200U 级别）设计，重点解决连续长按退格时的卡顿：

- 取色结果按 token 类型缓存，未命中同样记录，避免每次按键重扫数百条主题规则。
- 每次迸发只解析一次颜色，不再逐粒子查询主题表。
- 语法分类限定在光标前 6000 字符窗口内回看，超长行与大文件不再拖慢输入。
- 装饰器配置对象逐粒子复用，每帧只改写 CSS 字符串；200 粒子 × 30fps 下原先每秒约产生 1.8 万个临时对象，现趋近于零。
- 帧内按锚点 offset 缓存 `Range`，同一帧共享锚点的粒子不再重复 `positionAt` / `lineAt`。
- 状态栏文本未变更时跳过写入，悬停提示按 150ms 合帧刷新，减少跨进程 IPC。
- 帧循环按真实经过时间推进物理，不再按名义帧率假死换算；同时对 `setTimeout` 做自校正排程，抵消帧间隔的误差累积，连续输入时动作更均匀丝滑。
- 单帧时间步长上限 100ms：切回标签页 / 断点恢复等长间隔后不做补帧，粒子不会瞬移。
- 输入压力降载：0.5 秒内编辑达到 8 次（约 16 次/秒，高于人手连打、接近系统按键重复率）即判定为长按 / 连打，自动降到 30 帧档、粒子数与单次迸发量各砍一半、放大渲染量化步长、暂停连击放大与行抖动；输入一停当帧即自动恢复全质量。
- 渲染预算按帧率归一：渲染开销 ≈ 粒子数 × 帧率，故 60fps 下粒子上限收缩为 30fps 时的 60%（下限 0.6）。此前「调高帧率」会等比例放大每帧的装饰器推送量，这正是 60fps 反而比 30fps 更容易卡的原因；现在高帧率换来的是更顺的采样而非更高的负载。30fps 及以下行为不变。

### 修复

- 修复特效全部挤在行首的问题：定位改用伪元素自身的 `position: relative` 偏移，不再依赖编辑器内部的定位上下文。
- 修复 code 配色模式下「白字迸出蓝粒子」的颜色错位：轻量词法无法判断一个标识符在主题里究竟算 `variable` 还是普通文本（纯文本 / Markdown 里的每个词都会被判成 `variable`），而主题通常给 `variable` 单独上色（如 Dark Modern 的 `#9CDCFE` 浅蓝）。这类没把握的标识符现在统一回退到主题的默认前景色 `editor.foreground`（普通文本实际显示的颜色），做到「字是什么颜色，特效就是什么颜色」。
- 修复连按删除时特效漂移到后续行行首的问题：粒子的锚点是绝对字符偏移，而删除会让后面的文本整体左移，旧粒子因此相对文本向右漂移、漂过换行落到下一行行首。现在文档每次改动都会按 `contentChanges` 把驻留粒子的锚点从旧坐标映射到新坐标。

[0.1.0]: https://github.com/sqlsec/vscode-powermode/releases/tag/v0.1.0
